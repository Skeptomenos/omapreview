# OCR interface acceptance — 2026-09-16

The combined candidate passed the required suite and independent live acceptance.

The user authorized opt-in OCRmyPDF implementation across the GUI, CLI and MCP.
The shared engine and optional installation passed [Wave 1 acceptance](ocr-wave1-2026-09-16.md).
This record covers the adapters, native controls, agent guidance and combined integration.

## Accepted code

Accepted base: `230cf863505ea5d3ac17f5f5a346251ea8a69917`.
Corrected combined candidate: `d12aeb2b2e556dd946acd461ea124d3d857d5407`.
O3: `a1efa90`, `43cec8e`. O4: `f3825b5`, `7e4914a`, `540fc0b`, `a841a4e`, `9185bc1`.

- CLI OCR supports dependency discovery, source-bound preflight, explicit execution, progress and signal cancellation.
- MCP supports the same engine, progress and request/EOF cancellation. Ordinary PDF work remains off the event loop.
- The editor provides Recognize text, installed-language/page selection, Review, progress, cancellation and a new saved copy. Dirty state and undo history are protected. Incomplete recognition keeps the source open and offers an explicit Open copy action.
- The editor bridge exposes start/status/cancel through both public interfaces. Protocol errors stay distinct from structured OCR task errors.
- Progressive skill recipes require saved read/render/hash checks. Missing or incorrect text fails the expected postcondition.

## Final validation

The final suite ran from the integration checkout:

```sh
bwrap --bind / / --dev-bind /dev /dev --tmpfs /home/david/Downloads \
  --unsetenv OMEPREVIEW_SIGNATURE_DIR --unsetenv OMAPREVIEW_OCRMYPDF \
  --setenv XDG_CONFIG_HOME /home/david/.local/state/omapreview/coordination/evidence/ocr-wave2-final/private-config \
  --setenv OMAPREVIEW_TEST_OCRMYPDF /home/david/.codex/worktrees/omapreview-parity-ocr-integration/omapreview/.venv/bin/ocrmypdf \
  -- .venv/bin/python -m pytest tests/ -q
```

Result: **627 passed, no skips, exit 0**, in 197.18 seconds. The 422 warnings were GI/Python deprecations. Exact invocation and result: `ocr-wave2-final/command-result.json`; full output: `pytest.log`.

The independent corrected live run passed on the same candidate. CLI preflight and MCP start returned the canonical task record. Both interfaces returned completed status and preserved a failed task's destination-conflict details. Source/saved reads and renders matched actual PDF hashes. All three pages kept their geometry and pixels, including an unselected digital control and an unselected scan. The editor kept the source open.

A 24-page run completed before cancellation; late cancel preserved completion. A 96-page retry had live OCRmyPDF and Tesseract workers. Cua moved from page 1 to page 2 while recognition remained active. MCP cancellation then reached cancelled status through both interfaces, with null output/result and intact error details. The source was unchanged; worker PIDs, staging and cancelled destination were absent. All owned editors and the driver exited. Thirteen sampled task calls took at most 0.357 seconds, including CLI startup. This is not a numerical GTK latency claim.

Saved three-page source SHA-256: `f09510eb3af631eca3797a565537c84baecb041607a876f014dc3c2424e98412`.
Saved output SHA-256: `6121dea7920b02343a5945ad76c6af63c04d7b4726f1cf8388dd27695ef5bd7f`.
Root reviewed the saved MCP page image and the native 2/96 source capture.

Environment: Linux aarch64, Python 3.14.7, PyMuPDF 1.28.2, OCRmyPDF 17.11.0, MCP 2.2.0.
All final GUI/model checks use the shared lease and private Downloads/XDG paths; HOME stays unchanged.
Only synthetic PDF and signature fixtures are used.

The earlier combined `a2c4e15` suite passed 623 tests, but live acceptance found the task/error envelope bug. That run is a superseded checkpoint, not the final acceptance. The corrected socket regression covers complete, failed and cancelled task records plus real protocol errors. Development runs that missed the prescribed isolation are recorded separately and do not supply final isolated acceptance.

O3 independently passed real CLI SIGINT/SIGTERM and MCP cancellation/EOF cleanup, structured errors, deadlines and saved-output corpus checks. Fresh CLI-only and MCP-only agent tasks used only the public skill and their chosen interface. They verified selected-page OCR and a follow-on highlight. Both rejected missing text and failed the expected amount check on a poor scan (`123.45` was recognized as `12545`). Independent review verified that late cancellation preserves an already published result and that ordinary MCP discovery stays responsive.

O4 native Cua checks covered clean OCR/search, dirty-dialog Cancel, missing backend, live cancellation, page navigation during OCR, explicit changed-session opening, no-text opening and active dependency-query cleanup on close. Independent probes verified tracked thread cancellation and truthful completion summaries.

Unchanged engine, multilingual and installer checks are reused from Wave 1. The optional MCP minimum follow-up `230cf86` passed packaging tests, generated Arch metadata comparison, resolver rejection of MCP 1.2 and installation of 2.2. The corrected candidate also built a wheel. Its OCR/GUI/CLI/MCP modules matched the source bytes, and both user entrypoints were present. Wheel SHA-256: `3c810bbca95148b933af4958458f3bf7acf2c87aadbe61dd01f2b9638e3d4a5f`.

Raw commands, protocol frames, assertions, hashes and PNGs are retained under
`/home/david/.local/state/omapreview/coordination/evidence/ocr-wave2-final/`,
`ocr-wave2-integration/native-review/corrected-run/`, `ocr-o3/` and `ocr-o4/`.
The live result is `corrected-run/RESULTS.md`; `verification.json` owns its machine checks.
The shared plan is linked from the [project index](../../index.md).

## Limits and distribution boundary

Recognition requires review. A nonzero word count does not prove transcription accuracy. The [operation contract](../ops.md#ocr) owns unsupported-content refusals, preservation guarantees and cooperative cancellation limits. Hard parent death is not a cleanup guarantee.

Native dirty Save/Discard choices, close during active recognition, unsupported-file failure and editing pending operations during OCR were not all exercised through Cua. The retained reports distinguish native evidence from model/engine coverage.

No release was published or tag moved. Published v0.1.1 remains at `e6d6f2494cd9c7a831b898dce8b9aff20ff048e7`. No host installation command was run. OCR remains opt-in current-source functionality.
