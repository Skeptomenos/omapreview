# OCR engine and optional installation acceptance — 2026-09-16

The user authorized OCRmyPDF implementation in coordinated worktrees. This record
covers the shared engine and optional installation. It does not establish the
later CLI/MCP lifecycle adapters or native OCR controls.

## Accepted code

Base: `c623c738297b95addb513bba94adc5e009d38a5a`.
Combined tested candidate: `439725b282f7a4e4463e248ad5fafb98b20a61ca`.

- O1 engine: `ee6ed4a`, `ce00575`, `e5bff18`.
- O2 packaging: `db6eaac`.
- Integration commits: `8d1d6e6`, `70fb204`, `25f4366`, `439725b`.

The standalone OCR operation requires a new destination, explicit execution and
the inspected source hash. It validates saved bytes before publishing. The
[operation contract](../ops.md#ocr) owns its inputs, preservation checks and limits.
OCR remains optional; base GUI/CLI and optional MCP installation stay independent.

## Validation

The combined checkout ran the required suite with the installed OCRmyPDF 17.11.0
backend, system GI and PyMuPDF 1.28.2, and MCP 2.2.0:

```sh
bwrap --bind / / --dev-bind /dev /dev --tmpfs /home/david/Downloads \
  --unsetenv OMEPREVIEW_SIGNATURE_DIR --unsetenv OMAPREVIEW_OCRMYPDF \
  --setenv XDG_CONFIG_HOME /home/david/.local/state/omapreview/coordination/evidence/ocr-wave1-integration/private-config \
  --setenv OMAPREVIEW_TEST_OCRMYPDF /home/david/.codex/worktrees/omapreview-parity-ocr-integration/omapreview/.venv/bin/ocrmypdf \
  -- .venv/bin/python -m pytest tests/ -q
```

Result: **593 passed, no skips, exit 0**, in 135.18 seconds. The 408 warnings were
GI/Python deprecations. HOME stayed unchanged; private Downloads and XDG config
isolated signature and clipboard fixtures. No personal PDFs or signatures were used.

O1's final absent-backend suite on `e5bff18` passed **575 tests**, with 16 deliberate
real-backend skips, exit 0. Its retained real corpus passed all ten eligible cases;
nine refusal cases and source/output races have no-output regression coverage.
Existing installed CLI and stdio MCP journeys exercised discovery, preflight,
source-bound execution, independent read/render and saved-output hashes.

Two independent reviews reproduced and then verified corrections for shifted
hidden text, automatic third-party plugin loading and resource-error classification.
Their final selections passed 4 preservation and 18 lifecycle checks, including
real OCR geometry and real Tesseract cancellation. No scoped blocker remained.

Root ran actual installed CLI OCR in Afrikaans and combined English/Afrikaans on
a synthetic two-page scan. Both cases passed expected text, saved SHA-256, all five
page boxes, rotation, exact 100-DPI RGB renders and finite word-bound checks. The
source hash stayed unchanged. Saved PNG output was also visually inspected.

O2's suite passed 481 tests. Isolated base/OCR/MCP/both installs, upgrade and
uninstall boundaries passed. Checkout installation, a real Arch archive and a
base-only native launch were checked. Package-manager actions were simulated;
the host app was not replaced. Root repeated the packaging checks and shell syntax.

Detailed commands, reports, hashes and synthetic artifacts are retained under
`/home/david/.local/state/omapreview/coordination/evidence/ocr-wave1-integration/`,
`ocr-o1/` and `ocr-o2/`. Their completion records distinguish final evidence from
superseded checkpoints. The [shared plan](../../index.md) links the live batch state.

## Limits and next gate

The first increment deliberately refuses unsupported content. Recognition always
requires review. Resource samples are not hard disk quotas; native PDF calls are
not interruptible within one call. Source and dependency checks do not lock out
unrelated changes. The operation contract states these bounds.

O3 still owns CLI signals, MCP request cancellation/EOF/progress, structured errors
and independent agent recipes. O4 owns native OCR controls and editor handoff.
Their combined acceptance follows in O5. No release was published, no tag moved,
and the installed v0.1.1 app remained unchanged.
